# ASynK Python 3 Dependencies
# Install with: python3 -m venv venv && venv/bin/pip install -r requirements.txt

# Core libraries (previously vendored)
demjson3>=3.0
iso8601>=2.1
simplejson>=3.0
vobject>=0.9

# Google People API (replaces old gdata library)
google-api-python-client>=2.0
google-auth-httplib2>=0.1
google-auth-oauthlib>=1.0

# HTTP client
httplib2>=0.20
