# ASynK Python 3 Dependencies
# ===========================
#
# Quick start (install everything):
#   python3 -m venv venv && venv/bin/pip install -r requirements.txt
#
# Selective install (only the backends you need):
#   pip install .[google]       # Google Contacts
#   pip install .[carddav]      # CardDAV (Radicale, Nextcloud, etc.)
#   pip install .[exchange]     # Exchange Online (Microsoft Graph)
#   pip install .[all]          # All backends
#
# Core libraries (always required)
demjson3>=3.0
iso8601>=2.1
simplejson>=3.0
vobject>=0.9
httplib2>=0.20
keyring>=25.0

# --- Optional: Google Contacts (pip install .[google]) ---
google-api-python-client>=2.0
google-auth-httplib2>=0.1
google-auth-oauthlib>=1.0

# --- Optional: CardDAV (pip install .[carddav]) ---
caldav>=1.3.0

# --- Optional: Exchange Online (pip install .[exchange]) ---
msal>=1.28
requests>=2.28
